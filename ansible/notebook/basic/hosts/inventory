# 定义一个 inventory, 其中:
# 	vm 					inventory名称
# 	ansible_host		主机地址, 可以为主机IP，域名或者SSH配置名(参见"ssh_config"文件)
# 	ansible_ssh_user	ssh登录名称

# Form1
# vm ansible_host=remote-server ansible_ssh_user=alvin

# Form2
# [vm]
# remote-server ansible_ssh_user=alvin

# Form3
[vm:vars]
ansible_ssh_user=alvin

[vm]
remote-server

[vm_vault:vars]
ansible_ssh_user=alvin
ansible_become=yes
ansible_become_method=sudo 
ansible_become_pass='{{ sudo_pass }}'

[vm_vault]
remote-server